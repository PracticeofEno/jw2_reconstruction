# 게임플레이 동기화 전수 점검 중간보고

작성일: 2026-08-14

대상 브랜치: `experiment/render-144fps`

배포 대상: `RankerOCPV_Win/ranker_rebuild.exe`

## 1. 요청 사항

이번 작업은 다음 세 가지 실사용 문제를 고치고, 같은 종류의 누락이 남지 않도록 플레이어가 조작할 수 있는 유닛 동작 조합을 전수 비교하는 것을 목표로 한다.

1. 일꾼의 건물 건설 메뉴를 연 뒤 다른 일꾼을 선택하면 기본 명령 화면으로 돌아가야 하지만 건설 메뉴가 유지되는 문제
2. 수송 유닛에서 하차한 유닛이 화면상 사라져 보이는 문제
3. Noxious 사용 뒤 튕김/동기화 오류가 발생하는 문제 (`error1.ply`)

추가 요청에 따라 스킬뿐 아니라 일반 공격, 직접 명령, 수송, 생산, 건설, 장비, 변신, 수확 등 플레이어가 발생시킬 수 있는 동작 조합을 원본 `ranker.exe`와 프레임 단위로 비교했다.

## 2. 현재 배포 상태

- 원본 실행 파일: `RankerOCPV_Win/ranker.exe`
  - SHA-256: `0C404D0501400ABCA680D5618AE02BA72B11EAEED1EC125CC4F7CC2DAD18DB5A`
  - 작업 전 기준 해시와 동일하며 수정하거나 교체하지 않았다.
- 현재 재구성 실행 파일: `RankerOCPV_Win/ranker_rebuild.exe`
  - SHA-256: `A22E3DD14B5BE706BD7381BD449BAFF206D1E1F5812DF0BDDF7BCCABAE0B7AF2`
- 현재 소스 기준 레이아웃 프로브를 다시 생성하고 `current_layout.json`을 갱신했다.
- CTest 18개 전부 통과했다.

현재 재구성 실행 파일 SHA는 전체 패리티 검사와 `error1.ply` 재현 검사를 모두 통과한 최종 빌드이다.

## 3. 최초 세 문제의 수정 상태

### 3.1 일꾼 변경 뒤 건설 메뉴가 유지되는 문제

선택 유닛이 바뀔 때 이전 유닛의 하위 명령 패널 상태가 남지 않도록 선택/명령 패널 재구성 경로를 수정했다. 관련 집중 회귀 테스트가 통과했다.

### 3.2 수송 유닛 하차 뒤 유닛이 사라져 보이는 문제

하차한 승객의 활성 목록, 점유 셀, 가시성에 필요한 런타임 상태가 원본 순서대로 복구되도록 수정했다. 수송 집중 회귀 테스트 및 수송 명령 조합 테스트가 통과했다.

### 3.3 Noxious 사용 뒤 튕김/동기화 문제

Noxious의 초기 0 피해 충격을 재구성판이 일반 피해 처리처럼 취급하면서 사망/효과 상태가 달라지는 문제를 먼저 수정했다. 이어서 `error1.ply`를 분석한 결과, 더 큰 동기화 차이는 매 스크립트 프레임마다 소유자/유닛 타입 카운터를 전부 재구축하던 재구성판의 추가 동작에서 발생했다. 원본은 이 카운터를 이벤트별로 증감하며 매 프레임 전체 재구축하지 않는다.

따라서 스크립트 단계, 생성 콜백, 효과 유닛 생성, 소유자 AI 틱 등에 들어 있던 비원본 전체 재구축을 제거하고 필요한 이벤트 지점에서만 카운터 미러를 갱신하도록 바꿨다.

- Noxious 개별 스킬 케이스는 원본과 일치했다.
- `error1.ply`의 최초 문제 구간(프레임 8970~9300)과 종료부(12380~12415)는 수정 뒤 원본과 일치했다.
- 최종 빌드에서 `error1.ply` 전체 재생을 완료했다. 드라이버가 연결된 프레임 3부터 종료 프레임 12415까지 12,069개 정렬 관측점이 모두 일치했고, 최초 차이는 발견되지 않았다.
- 압축 추적의 263개 간격(내부 미관측 프레임 344개, 최대 연속 8프레임)은 문제 발생 구간 집중 재생과 원샷 보강 검사로 별도 확인했다.

## 4. 전수 점검에서 추가로 발견하고 수정한 동기화 문제

최초 세 증상만 수정하고 끝내지 않고, 원본과의 조합 비교에서 발견한 아래 차이도 함께 수정했다.

### 유닛/명령 상태

- 기본 행동 사거리 계산 시 유닛 발자국 크기 반영 오류
- 직접 명령의 장비/반환 payload가 잘못된 런타임 필드를 덮어쓰던 오류
- 대상 상호작용 접근 중 저장 경로와 활성 명령 좌표가 원본과 다르던 오류
- 특수 타입의 대상 상호작용 사거리 계산에서 효과 보정값이 빠진 오류
- 화물 전달/비우기 명령이 잘못된 payload를 읽던 오류
- 생산 취소 뒤 활성 슬롯 컨텍스트가 남던 오류
- 반환 화물 명령의 fallback 상태 누락

### 장비/경험치

- 장비로 타입을 교체할 때 런타임 타입 플래그가 보존되지 않던 오류
- 직접 장비 명령 플래그가 실제 저장 장비 슬롯과 다르게 계산되던 오류
- 경험치 등급 상승 시 action `0x2d` 능력 부착 누락
- 장비 제거 가능 여부에서 정의 플래그 검사 조건이 원본과 다르던 오류

### 생성/사망/변신/연결 유닛

- 변신 유닛의 소유자 타입 카운트가 현재 표시 타입 버킷으로 잘못 집계되던 오류
- 연결 유닛 해제 중 화물/원본 타입 카운트가 잘못 감소하던 오류
- 특정 건물 사망 상태에서 원본에는 없는 소유자 카운터 감소가 일어나던 오류
- 저타입 유닛 사망 프레임의 생산 타입 미러가 1프레임 늦던 오류
- Resurrect와 Rebirth로 되살린 유닛의 타입 카운터 미러가 1프레임 늦던 오류

마지막 부활 문제는 유닛 자체, 위치, 체력, 효과 슬롯은 이미 같은 프레임에 일치했지만 생산/AI가 읽는 타입 카운터만 다음 프레임에 반영되던 문제였다. 부활 호출 안에서 생명주기, 생산, AI 미러를 동시에 게시하도록 수정했다.

## 5. 현재까지의 비교 결과

### 직접 명령 전수

- 37개 배치
- 964개 명령/유닛 조합
- 누락 0, 불일치 0, 정확 일치 964
- 현재 후보 SHA 결과 964, 실제 명령 상태 관측 964, 행동 공백 검증 실패 0

현재 후보 SHA에서 37개 고유 추적 아티팩트를 다시 생성해 전부 확인했다. ReturnCargo의 원본 특수 완료 규칙과 이동 불가 유닛의 정상 접근 대기 상태도 각각 실제 행동 증거로 판정했다.

### 스킬

- 스킬 매니페스트: 29개 유효 action/유닛 조합
- Noxious 개별 케이스 통과
- Resurrect (`A10U027`) 집중 재검증: 관측 지점 135개 정확 일치
- Rebirth (`A25U062`) 집중 재검증: 관측 지점 135개 정확 일치
- 현재 최종 후보 SHA에서 29개 전체 정확 일치
- 매니페스트 예상 29개, 현재 SHA 결과 29개, 누락 0, 미실행 0
- 압축 추적 공백 원샷 보강 프레임 65개 전부 정확 일치

스킬 판정기는 명령 실행 전후의 압축 추적만 비교하지 않는다. 명령 이후 관측 프레임 사이에 간격이 있으면 해당 프레임을 원샷으로 다시 재생해 짧게 나타났다 사라지는 1프레임 차이도 실패로 처리한다. 이 보강 검사로 Resurrect/Rebirth의 미러 지연을 발견했다.

### 자동 회귀 테스트

현재 빌드에서 CTest 18/18 통과:

- 선택, 수송, Noxious 집중 회귀
- 혼합 유닛 스킬 원본 패리티
- 장비 타입 교체/가시성
- 게임플레이 패킷 및 네트워크 회귀
- 게임 루프, 144 FPS 렌더, 애니메이션 생성
- 리플레이 저장 제어 등
- 효과 슬롯 연결 순회 및 Evil Portal 건설 해제 회귀

## 6. 최종 빌드에서 완료한 전체 비교 범위

다음 조합을 모두 원본/재구성판 프레임 추적으로 비교했다.

| 분류 | 예상 조합/케이스 |
|---|---:|
| 일반 공격 | 435 (현재 SHA 435/435 exact, 155프레임 연속) |
| 직접 명령 | 964 |
| 스킬 | 29 |
| 연결 유닛 해제 | 4 |
| 수확 | 4 |
| 이동/순찰 | 91 / 12 |
| 보조 선택 명령 | 54 |
| 수송 하차 | 219 |
| 실제 승차-하차 순환 | 192 |
| 기본/경비/주 명령 | 101 / 66 |
| 범위 토글 | 73 |
| 정의 그룹 선택 | 180 |
| 변신 | 5 |
| 건설 | 52 |
| 유닛 생산 | 69 |
| 생산 주문 바인딩 | 54 |
| 생산 취소 | 188 |
| 장비 직접 적용 | 8,499 |
| 장비 토글 | 9,924 |

여기에 `error1.ply` 전체 0~12415 프레임을 추가로 검증했다. 일부 테스트는 한 프로세스에서 여러 조합을 묶은 배치 방식으로 실행되며, 묶음 재생으로 명령 실행 여부를 판정하기 어려운 경우에는 격리 재생으로 보강했다.

일반 공격은 55개 묶음 배치 뒤 미실행 조합 43개를 단일 공격원/단일 목표 격리 리플레이로 보강했다. 목표 클래스 조합 435개가 모두 현재 후보 SHA에서 정확 일치했고 모든 케이스가 155개 정렬 프레임을 연속으로 기록했다. 공격 프로필 바인딩 166개 중 플레이 가능한 133개는 `exact`, 카탈로그상 플레이 불가한 33개는 `not_player_reachable`이며 `incomplete`는 0이다.

### 최종 빌드 SHA에서 추가 완료된 범위

- 연결 유닛 해제: 4/4, 325 정렬 프레임 `exact`
- 수확: 4/4, 225 정렬 프레임 `exact`
- 변신: 5/5, 65 정렬 프레임 `exact`
- 건설: 52/52, 배치마다 95 정렬 프레임 `exact`
- 유닛 생산: 69/69, 배치마다 85 정렬 프레임 `exact`
- 생산 주문 바인딩: 54/54, 배치마다 75 정렬 프레임 `exact`
- 생산 취소: 188/188 `exact`
- 이동/순찰: 91/91, 최종 보강 배치까지 115 정렬 프레임 `exact`
- 보조 선택 명령: 54/54, 최종 보강 배치까지 60 정렬 프레임 `exact`
- 기본/경비 명령: 경비/대기 101/101 및 주 지점 행동 66/66, 배치마다 95 정렬 프레임 `exact`
- 범위 토글: 73/73, 배치마다 55 정렬 프레임 `exact`
- 정의 그룹 선택: 180/180, 모든 케이스 155 정렬 프레임 `exact`
- 수송 하차: 실제 플레이 가능 212/212 `exact`, 중첩 수송으로 원본이 거부하는 7건 `not_player_reachable`, 전체 219건 모두 85 정렬 프레임 일치
- 실제 승차-하차 순환: 192/192, 모든 케이스 195 정렬 프레임 `exact`; 탑승과 하차 후 가시성 복구 모두 관측
- 장비 직접 적용: 8,499/8,499 `exact`; 압축 추적에서 동작 프레임이 비어 보인 51건도 격리 원샷 재생으로 실제 효과와 원본 일치를 확인
- 장비 토글: 9,924/9,924 `exact`; 동작 프레임 공백 51건도 격리 원샷 재생으로 확인

정의 그룹 선택의 최초 실행에서는 건물 대상이 포함된 배치가 명령 다음 프레임에 경로·방향 차이를 보였다. 조사 결과 제품 코드 차이가 아니라 생성한 진단 맵의 레코드 불일치였다. 건물 객체는 시나리오 객체 레코드에 추가했지만 원본 경로 탐색이 사용하는 레코드 13의 건물 발자국 점유 비트는 0으로 만들었고, 재구성본만 로드 시 점유를 복원해 두 실행 파일이 서로 다른 맵 상태를 사용했다. 생성기를 수정해 건물 크기별 발자국, 유닛 형식, 소유자 payload를 레코드 13에 함께 기록했다. 수정된 23개 배치를 전부 다시 실행한 결과 180개 조합 모두 현재 후보 SHA와 새 리플레이 해시에서 155프레임 연속 일치했다.

수송 하차 카탈로그 219건 가운데 7건은 수송 능력이 있는 유닛을 다른 수송 유닛의 승객으로 넣는 중첩 수송 조합이었다. 원본 탑승 진입 코드는 승객의 수송 능력 비트를 검사해 이 조합을 명시적으로 거부한다. 원본과 재구성본 모두 같은 거부 상태를 85프레임 동안 유지했으므로 `not_player_reachable`로 분리했고, 실제 플레이 가능한 하차 조합 212건과 승차부터 시작하는 192건은 모두 정확 일치했다.

## 7. 최종 검증 상태

- 정규화된 최종 감사 결과: 예상 21,369건, 기록 21,369건, 오류 0건
- 모든 플레이 가능 조합은 `exact`이며, 원본 자체가 명시적으로 거부하는 조합만 `not_player_reachable`로 분리했다.
- `error1.ply` 전체 연속 재생: 종료 마커까지 동기화 차이 없음
- 최종 빌드와 배포된 `ranker_rebuild.exe` SHA-256 일치
- 원본 `ranker.exe` SHA-256은 작업 전 기준과 동일
- 소스 및 진단 도구의 문법/회귀/패리티 점검 완료

이 문서는 사용자가 작업 도중 읽을 수 있도록 만든 중간보고서이며, 현재 내용에는 최종 전수 검증 결과까지 반영되어 있다.

## 8. 진단 아티팩트 위치

- 리플레이/프레임 비교 실행 결과: `ranker_reconstructed_code/tools/replay_debug/artifacts/runs`
- 현재 런타임 레이아웃: `ranker_reconstructed_code/tools/replay_debug/artifacts/current_layout.json`
- 플레이 조합 생성기/실행기: `ranker_reconstructed_code/tools/gameplay_parity`
- 정규화된 최종 감사 보고서: `ranker_reconstructed_code/tools/gameplay_parity/reports/final_parity_audit.md`

진단 PNG는 워크스페이스 규칙에 따라 `debug_artifacts/png` 아래에서만 관리하며, 이번 중간 단계에서는 새 진단 PNG를 만들지 않았다.
